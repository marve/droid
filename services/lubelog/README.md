# Purpose
Tracks vehicle maintenance and service logs using [LubeLogger](https://lubelogger.com/).

# Usage
The web interface is available at `http://your-host-ip:8080` after the service starts.

Data (config, records, documents, images) is persisted in named Docker volumes and survives container restarts.
